---
description: 数据驱动平衡：JSON 主数据 + Java 回退约定
globs: "**/data/evolutionary_mod/**/*.json,**/src/main/java/**/loot/**/*.java,**/src/main/java/**/system/sets/**/*.java"
alwaysApply: false
---

# 数据驱动平衡

改掉落、词条、套装数值时：**先改 JSON，再必要时补 Java 回退常量**。

> 本表是「某系统数值改哪里」的操作参考，按系统维护；数值的**完整联动关系**以 `docs/dev/修改同步索引表.md` 对应章节为准，新增系统时两处都要补。总览大纲（`project-overview.mdc`）不逐系统罗列。

## 对照表

| 系统 | 主数据 | Java 回退 |
|------|--------|-----------|
| 掉落 | `data/evolutionary_mod/loot/drop_table.json` | `loot/AccessoryDropTable.java` |
| 词条 | `data/evolutionary_mod/attributes/ranges.json` | `loot/AttributeRollRanges.java` |
| 套装 | `data/evolutionary_mod/sets/{key}_set.json` | `system/sets/SetBalanceConfig.java` |
| 铸币/徽章掉落 | `data/.../currency/coin_drops.json`、`badge_drops.json`（新） | GLM 独立判定，无饰品回退；要吃幸运与 Looting 规则（见 `CURRENCY_TRADE_SYSTEM_V1.md`） |
| 密封匣/材料包 | `data/.../currency/pouch_rolls.json`、`material_pouch.json`（新） | roll-on-open 复用标准滚动（`AccessoryDropTable`），保底参数在 JSON |

维护掉落时同步更新 `drop_table.template.json`。

## 必须遵守

- 百分比用小数（`0.05` = 5%）
- 掉落条目：`loot_table_id`、`drop_chance`∈[0,1]、`allowed_rarities`（大写枚举）
- 无效条目：打 `[ClassName]` 日志并计入 invalid，禁止静默吞掉
- 回退命中：默认 warn；`Config.isStrictDataDriven()` 为 true 时抛异常
- 验证：`/acc validate`、`/acc drop|attr|set query`

## 禁止

```text
❌ 只在 Java 硬编码新平衡数值却不写 JSON
❌ 恢复已删除的 loot_tables/accessories/pools/*.json 池表模式
❌ 词条/套装 JSON 缺字段却不记 invalid/fallback
```

---
description: 配置、双语 lang 与 docs 维护约定
globs: "**/Config.java,**/WeightConfig.java,**/assets/evolutionary_mod/lang/*.json,docs/**/*.md"
alwaysApply: false
---

# Config / Lang / Docs

## Config

- 用 `ModConfigSpec.Builder` + `comment` + `defineInRange` / `defineListAllowEmpty`
- `ModConfigEvent` 时拷到运行时字段；非法条目丢弃并打日志
- 主类 `registerConfig(COMMON, *.SPEC)`：根 `Config` + 子系统配置（如 `WeightConfig`）
- 子系统专用配置放子系统包，不要全塞进根 `Config`
- 数据驱动严格模式：`strictDataDriven`（`Config.java`）

## Lang

- 同步维护 `zh_cn.json` 与 `en_us.json`
- 键：`item.evolutionary_mod.{id}` + `.description`；tooltip/slot/set/key 用现有前缀
- 新物品必须双语各加一对键

## Docs（`docs/README.md`）

| 目录 | 用途 |
|------|------|
| `docs/planning/` | 策划案：`{FEATURE}_SYSTEM_V{n}.md` |
| `docs/player/` | 玩家指南 |
| `docs/dev/` | 开发者「改哪里」对照 |

- 一份事实一处维护（快捷键、槽位数、指令）
- 列表优先表格，便于追加
- 大改玩法后更新 `docs/dev/开发者指南-精简版.md`
- **`docs/dev/修改同步索引表.md` 是强制收尾检查项**：改任何配置/数据/物品/实体相关文件后，按表核对联动文件；表本身在依赖关系变化时同步更新（新增系统/文件后补一行）
- 新增系统（锻造/材料/货币交易等）时，优先补齐 `docs/dev/修改同步索引表.md` 对应条目 + 对应策划案「项目规范检查」小节，再改代码

### 策划案结尾必须附「项目规范检查」

> 新建/大改任一策划案（`docs/planning/**`）时，在文档**末尾**追加一个「项目规范检查」小节，用检查表逐行核对：**本策划案是否会引起下列项目规范变动**。

| 项目规范 | 核对要点 | 状态 |
|----------|----------|------|
| `docs/dev/修改同步索引表.md` | 是否新增系统/数据文件 → 需补联动表章节 | ⬜ 需改 / ⬜ 不变 |
| `docs/dev/开发者指南-精简版.md` | 是否新增玩法模块 → 需补「改哪里」入口 | ⬜ 需改 / ⬜ 不变 |
| `.cursor/rules/*.mdc` | 是否有新包结构/命名/数据驱动/同步约束 | ⬜ 需改 / ⬜ 不变 |
| 双语 lang（`zh_cn.json` / `en_us.json`） | 是否新增物品/实体/引导书/消息 key | ⬜ 需改 / ⬜ 不变 |
| Patchouli 引导书（`patchouli_books/guide/**`） | 是否新增分类/条目/文案 | ⬜ 需改 / ⬜ 不变 |
| `docs/progress/`（进度文档） | 是否新增模块进度条目 | ⬜ 需改 / ⬜ 不变 |
| `docs/README.md`（文档目录） | 是否新增策划案文档 | ⬜ 需改 / ⬜ 不变 |
| `docs/player/`（玩家指南） | 是否新增玩家可感知玩法 | ⬜ 需改 / ⬜ 不变 |
| 配置文件示例（`docs/planning/config/`） | 是否新增 TOML 配置示例 | ⬜ 需改 / ⬜ 不变 |

- 每个「需改」行必须写清**具体要改动的文件与内容**；「不变」行打勾即可。
- 实装前若尚未落地，状态标「需改（实装后）」；一旦实装，勾选后即与项目规范保持同步。
- 检查表置于策划案正文末尾、总结之前；策划案修订版本号递增时同样要复查此表。

## 禁止

- 只改中文 lang 或只改代码不更新开发者对照表
- 在多处文档复制会漂移的关键数字（应引用单一来源）
- 收尾时不查 `docs/dev/修改同步索引表.md` 就提交任务
